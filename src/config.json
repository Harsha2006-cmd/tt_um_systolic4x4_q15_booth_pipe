{
  "//": "User-supplied LibreLane config overrides for this project. This file is intentionally left mostly empty -- do NOT change DIE_AREA/FP_DEF_TEMPLATE or other density-affecting keys here, since all TT projects in a shuttle are hardened together against a fixed tile size. Anything you add here is merged on top of the auto-generated src/user_config.json into src/config_merged.json.",

  "KLAYOUT_XOR_IGNORE_LAYERS": ["235/4"]
}
